export enum DirectionWithDefinedStart {
    Up = 1,
    Down,
}

export enum Direction {
    Up,
    Down,
}
export enum DirectionString {
    Up = 'Up!',
    Down = 'Down!',
    Left = 'Down!' + '90d',
}
